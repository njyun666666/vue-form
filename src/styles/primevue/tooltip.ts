import type { TooltipDesignTokens } from '@primeuix/themes/types/tooltip'

const tooltip: TooltipDesignTokens = {
  colorScheme: {
    dark: {
      root: {
        background: '{neutral.300}',
        color: '{neutral.950}'
      }
    }
  }
}

export default tooltip
